import { BlockInvalidNumberKeysDirective } from './block-invalid-number-keys.directive';

describe('BlockInvalidNumberKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockInvalidNumberKeysDirective();
    expect(directive).toBeTruthy();
  });
});
